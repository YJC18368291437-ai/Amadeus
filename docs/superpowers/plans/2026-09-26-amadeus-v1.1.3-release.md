# Amadeus v1.1.3 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task with review checkpoints.

**Goal:** Publish Amadeus v1.1.3 with the Android save-race conflict fix while preserving the already published v1.1.2 release.

**Architecture:** Keep the existing four plugins and Docker deployment. Promote the tested editor save-baseline fix into the next patch version, update the PWA cache namespace with the release, regenerate release artifacts, and deploy the same image to port 3080 before publishing the GitHub Release.

**Tech Stack:** Node.js 24, npm workspaces, Node test runner, esbuild, Docker, GitHub CLI.

---

### Task 1: Synchronize v1.1.3 metadata

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `packages/editor/package.json`
- Modify: `packages/files/package.json`
- Modify: `packages/login/package.json`
- Modify: `packages/reader/package.json`
- Modify: `packages/login/src/pwa.mjs`
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [x] **Step 1: Bump all project packages and PWA cache to `1.1.3`.**

Run `npm version 1.1.3 --no-git-tag-version --workspaces`; then update the root package version and `PWA_VERSION` to `1.1.3`, verifying all five package manifests and the lockfile agree.

- [x] **Step 2: Update current README references and add the v1.1.3 changelog entry.**

Change only current-version, clone/upgrade tag, and current artifact references to `1.1.3`; leave historical v1.1.1/v1.1.2 entries unchanged. Document that completed editor saves now acknowledge their disk version and avoid false external-change warnings on Android/slow clients.

### Task 2: Validate and package

**Files:**
- Modify: `.release/` generated artifacts

- [ ] **Step 1: Run tests, build, and whitespace validation.**

Run `npm test`, `npm run build`, and `git diff --check`; expect all tests to pass, including the completed-save regression.

- [ ] **Step 2: Generate and verify release packages.**

Run `npm run pack:plugins`; verify exactly four `1.1.3` archives, `SHA256SUMS`, matching checksums, and PWA assets in the Login package.

### Task 3: Deploy and publish

**Files:**
- Generated Docker image: `amadeus:1.1.3-android-save-fix-20260926`

- [x] **Step 1: Build and replace the running 3080 container.**

Run `docker build -t amadeus:1.1.3-android-save-fix-20260926 .`; replace the current container while preserving its `/data` volume, workspace mount, config mount, and `127.0.0.1:3080` binding; retain the prior container as rollback.

- [x] **Step 2: Verify the live service.**

Check authenticated home page, manifest, Tailscale HTTPS access, and protected `/amadeus/ping`; require the container to remain running without restarts.

- [x] **Step 3: Commit, tag, push, and publish the Release.**

Commit the release as `chore: release Amadeus 1.1.3`, create annotated `v1.1.3`, push `main` and the tag, then create the GitHub Release with the four archives and `SHA256SUMS`.

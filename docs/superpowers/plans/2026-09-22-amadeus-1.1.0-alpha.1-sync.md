# Amadeus 1.1.0-alpha.1 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Promote the verified desktop Amadeus changes into the GitHub repository as `1.1.0-alpha.1`.

**Architecture:** Treat `C:/Users/11588/Desktop/Amadeus/app` as the source of truth for tracked application code. Synchronize only repository-owned source, tests, docs, build metadata, and package manifests; preserve local configuration, sessions, dependencies, and generated output. Remove obsolete alpha.1 modules that the desktop alpha.2 build no longer uses, then regenerate the lockfile and validate the complete tree.

**Tech Stack:** Node.js 24, npm workspaces, React 18, DSH `0.1.6-alpha.2`, Node test runner, Git, GitHub origin.

---

### Task 1: Synchronize Desktop Source Changes

**Files:**
- Modify: `package.json`, `package-lock.json`, `CHANGELOG.md`, `README.md`, `amadeus.example.yml`
- Modify: `packages/files/**`, `packages/login/src/auth.mjs`, `packages/reader/**`, `scripts/**`, `tests/**`
- Remove: obsolete reader conversion/cache files, terminal package files, and tests absent from the desktop source
- Preserve: `docs/assets/amadeus-home.png`, `amadeus.local.yml`, `.amadeus/`, `.release/`, `node_modules/`, package `dist/`, and desktop-only `dsh-home/`

- [x] **Step 1: Audit the desktop tree against `origin/main`.**
  Record the 45 tracked-file differences, including DSH alpha.2 compatibility, native preview integration, artifact writes, navigation authentication, and the removed legacy terminal/conversion modules.
- [x] **Step 2: Mirror repository-owned files from the desktop app.**
  Copy the desktop app's tracked source, tests, docs, scripts, UI assets, and manifests into `D:\Projects\Amadeus`; exclude runtime/configuration directories and generated dependencies/artifacts.
- [x] **Step 3: Remove tracked files that the desktop source no longer contains.**
  Remove the obsolete `packages/reader/src/{convert.mjs,latex-preview.jsx,loading.css,loading.jsx,onlyoffice.mjs,page-control.css,page-control.jsx,pdf-http.mjs,preview-cache.mjs,scroll-page.mjs}`, `packages/terminal/**`, and their corresponding obsolete tests. Keep `docs/assets/amadeus-home.png` because the repository README references it even though the deployment copy omits documentation assets.

### Task 2: Set the Pre-release Version

**Files:**
- Modify: `package.json`
- Modify: `packages/reader/package.json`
- Modify: `package-lock.json`
- Modify: `README.md`, `CHANGELOG.md`

- [x] **Step 1: Set package versions to `1.1.0-alpha.1`.**
  Set the workspace and Reader package versions to `1.1.0-alpha.1`; keep Files at `1.0.3` and Login at `1.0.1`; use DSH `0.1.6-alpha.2`.
- [x] **Step 2: Regenerate the lockfile.**
  Run `npm install --package-lock-only --ignore-scripts` and verify the root and Reader lockfile versions and DSH alpha.2 entries.
- [x] **Step 3: Document the release.**
  Add a `1.1.0-alpha.1` changelog entry covering alpha.2 compatibility, native document previews, LaTeX artifact writes, annotation/session compatibility, and removal of obsolete conversion/terminal paths; update README version badges and DSH version text.

### Task 3: Verify, Commit, and Publish

**Files:**
- Modify: all files from Tasks 1-2
- Create: Git tag `v1.1.0-alpha.1`

- [x] **Step 1: Run repository checks.**
  Run `npm test`, `npm run build`, `node --test tests/files.test.mjs`, `git diff --check`, and confirm `git status` contains only the intended release changes.
- [x] **Step 2: Commit the release.**
  Create commit `feat: release amadeus 1.1.0-alpha.1` after reviewing the staged summary.
- [x] **Step 3: Publish GitHub state.**
  Push `main` and tag `v1.1.0-alpha.1` to `origin`; verify the remote branch and tag resolve to the release commit.

# Amadeus v1.1.0 release plan

**Goal:** Publish the validated code-server release as stable `v1.1.0`, with a concise Docker deployment guide and matching plugin artifacts.

**Architecture:** Keep the existing four Cordis plugins and Docker Compose deployment. Remove stale source coupling from the file tree, isolate the conversation collapse UI, update every workspace package version, then validate the exact release source and build output before publishing the main commit and tag.

**Stack:** Node 24, DSH 0.1.6-alpha.2, code-server 4.104.2, Docker Compose, TeX Live, GitHub Releases.

- [x] Move the file editability predicate into the files plugin and extract the conversation collapse UI into a focused reader module.
- [x] Rewrite README with a clean-install Docker path, Windows PowerShell commands, volume backup and upgrade, HTTPS proxy requirements, everyday usage, and release verification.
- [x] Set root and four plugin versions to `1.1.0`, synchronize `package-lock.json`, and write the stable release changelog.
- [x] Run unit tests, editor browser regression, plugin build/pack, Compose config validation, and a clean Docker image build and launch.
- [ ] Review the release diff, commit, fast-forward `main`, push, create annotated `v1.1.0` tag, and publish a GitHub release with the four plugin archives and checksums.

# Test documents

`lecture.docx` and `lecture.pptx` are synthetic, two-page documents containing short explanations of derivatives and gradient descent. They contain no user documents or credentials.

The ONLYOFFICE integration test converts both files and verifies two pages and extractable text with PDF.js. Neither fixture requires a model connection.
